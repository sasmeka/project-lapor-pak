version: '3.8'

services:
  laravel-app:
    build: .
    image: laravel_project_lapor_pak_image
    container_name: laravel_project_lapor_pak
    ports:
      - "8808:8000"
      - "8809:8809"
    volumes:
      - .:/var/www
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"

    environment:
      APP_NAME: Laravel
      APP_ENV: local
      APP_KEY: base64:MW15XMb4ZRD1IpiyCBjkmh/ZA1vYu83UOIwmqZ/Ovtw=
      APP_DEBUG: "true"
      APP_URL: http://localhost:8808

      DB_CONNECTION: mysql
      DB_HOST: host.docker.internal
      DB_PORT: 3306
      DB_DATABASE: project_lapor_pak
      DB_USERNAME: root
      DB_PASSWORD: nasywa2008

      SESSION_DRIVER: database
      CACHE_STORE: database
      QUEUE_CONNECTION: database

      BROADCAST_DRIVER: reverb
      BROADCAST_CONNECTION: reverb

      REVERB_APP_ID: 158897
      REVERB_APP_KEY: zpjwhgbv9tqgst6g4iys
      REVERB_APP_SECRET: fd9jkd605yxbhf9mlrkf
      REVERB_HOST: 0.0.0.0
      REVERB_PORT: 8809
      REVERB_SCHEME: http

    command: >
      sh -c "php artisan migrate --force &&
         php artisan reverb:start --host=0.0.0.0 --port=8809 &
         php artisan serve --host=0.0.0.0 --port=8000"